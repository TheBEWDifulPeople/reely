<% if @playlist.errors.any? %>
<h2>errors prevented this playlist from being saved!</h2>
    
  <ul>
  <% @playlist.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>

<% end %>

<%= form_for @playlist, :html => {:multipart => true} do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.label :mp3 %>
	<%= f.text_field :mp3 %>
	<%= f.file_field :track %>
	<%= f.submit%>
<% end %>